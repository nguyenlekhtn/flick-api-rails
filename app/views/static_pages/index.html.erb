<div class="bg-white pt-8">
  <div class="max-w-2xl mx-auto">
    <%= form_with url: static_pages_path, method: :get, html: { class: "flex flex-col items-center gap-y-6"} do |form|  %>
      <%= form.text_field :user_id, class: "w-full border border-indigo-950 rounded-lg shadow-lg px-8 py-4 focus:outline-none focus:border-none focus:bg-gray-100 focus-within:ring focus-within:ring-blue-500", placeholder: "A Flick's User Id. Eg: 191340088@N06" %>

      <%= form.submit "Look for photos", class: "bg-orange-500 shadow-lg hover:bg-orange-700 rounded-full font-medium px-8 py-4 text-white" %>
    <% end %>
  </div>
</div>

<div class="bg-white pt-12">
  <div class="container mx-auto">
    <% if @photo_urls %>
      <div class="flex flex-wrap justify-center">
        <% @photo_urls.each do |photo|  %>
          <%= link_to photo[:url_photopage], class: "shrink" do %>
            <%= image_tag photo[:url] %>
          <% end %>
        <% end %>
        <%=  %>
      </div>
    <% end %>
  </div>
<div class="container mx-auto">

